#include <iostream>
#include <cstring>

int main()
{
	//Menu prototype. This will change into a class later on.
	std::string command;
	std::cin >> command;
	while (command != "Exit")
	{

	}
	std::cout << "Exiting the program..." << std::endl;
}